import clamp from './clamp'
import lerp from './lerp'
import Vector from './vector'

const { abs, acos, asin, atan, atan2, ceil, cos, exp, floor, log, max, min,
 pow, random, round, sin, sqrt, tan, E, LN2, LN10, LOG2E, LOG10E, PI, SQRT1_2,
 SQRT2 } = Math

/******************************************************************************/
// Exports
/******************************************************************************/

export { abs, acos, asin, atan, atan2, ceil, cos, exp, floor, log, max, min,
 pow, random, round, sin, sqrt, tan, E, LN2, LN10, LOG2E, LOG10E, PI, SQRT1_2,
 SQRT2
}

export { clamp, lerp, Vector }
